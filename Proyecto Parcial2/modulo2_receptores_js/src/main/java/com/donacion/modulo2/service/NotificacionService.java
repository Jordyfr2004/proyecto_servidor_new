//package com.donacion.modulo2.service; 

//import org.springframework.stereotype.Service;

/**
 * NotificacionService:
 * TEMPORAL: Todo el código de notificaciones está comentado según instrucciones del docente.
 * Se activará cuando sea necesario después de las pruebas en Apollo.
 */
//@Service
//public class NotificacionService {

    /*
    public void notificarWebSocket(String mensaje) {
        String url = "http://localhost:3000/notify";
        RestTemplate restTemplate = new RestTemplate();
        HttpHeaders headers = new HttpHeaders();
        headers.setContentType(MediaType.APPLICATION_JSON);
        String jsonBody = "{\"mensaje\":\"" + mensaje + "\"}";
        HttpEntity<String> request = new HttpEntity<>(jsonBody, headers);
        restTemplate.postForEntity(url, request, String.class);
    }
    */
//}
